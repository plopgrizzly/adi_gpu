[package]
name = "minimal"
version = "0.1.0"
authors = ["Jeron Aldaron Lau <jeron.lau@plopgrizzly.tech>"]

[dependencies]
willow = { path = "../../" }
aci_png = "^0.2"

# Enable checks & validation
[features]
default = []
checks = ["willow/checks"]
validation = ["willow/validation"]
